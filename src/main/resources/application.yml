server:
    address: 0.0.0.0
    port: 8081
    error:
        include-message: always
        include-binding-errors: always
spring:
    datasource:
        url: jdbc:postgresql://dpg-d326kvffte5s7386s2k0-a/aliencloud_postgres_render
        password: YbAS8c0GDCBz4mUnCpuEsVtxthNVEkj3
        username: aliencloud_postgres_render_user
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
                show_sql: true
    servlet:
        multipart:
            max-file-size: 10MB  # Sets the maximum size for a single file
            max-request-size: 10MB # Sets the maximum size for the entire request
